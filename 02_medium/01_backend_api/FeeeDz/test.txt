comando provato:
curl -X POST -H "Content-Type: application/json" -d '{"array": [3, 1, "apple", 2, "banana"]}' http://localhost:5000/sort

risultato:

{
  "sorted_array": [
    "apple",
    "banana",
    1,
    2,
    3
  ]
}
